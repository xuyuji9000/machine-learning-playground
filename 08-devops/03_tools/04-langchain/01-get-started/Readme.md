This folder documents langchain **Get Started** example.

# Commands

1. Setup python virtual environment

    ``` shell
    virtualenv -p python3.12 venv

    pip3 install -r ./requirements.txt
    ```

2. Start Jupyter Lab

    ``` shell
    jupyter-lab
    ```
